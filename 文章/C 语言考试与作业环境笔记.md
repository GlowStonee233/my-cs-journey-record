

> 适用人群：  
> - 从未接触过 Linux / WSL  
> - 正在复习 C 语言期末 / 上机考试  
> - 平时需要完成 C 语言作业  
---
# 第一部分：WSL 与 Linux 基础（环境篇）
## 1. 什么是 WSL？
**WSL（Windows Subsystem for Linux）**  
👉 在 Windows 中直接运行真正的 Linux（如 Ubuntu）
### 使用 WSL 的好处
- 使用真实的 `gcc / gdb / apt`
- 环境接近学校机房 / OJ / 考试
- 不用虚拟机，不用重启
- 与 VS Code 深度结合
---
## 2. 启动与进入 WSL（Windows 中）
```powershell
wsl
# 启动 Linux（Ubuntu）终端
````

```powershell
wsl -l -v
# 查看已安装的 Linux 系统及 WSL 版本
```
> VERSION = 2 表示使用的是 WSL2（推荐）
---
## 3. Linux 的基本概念（一定要知道）
### 3.1 没有 C: / D:
- `/`：根目录
- `/home/用户名`：你的个人目录（最重要）
✅ **写代码只放在 /home**
---
### 3.2 普通用户 vs 管理员（sudo）
```bash
sudo 命令
# 以管理员权限执行命令
```
⚠️ 输入密码时：
- 不显示任何字符
- 是正常现象
---
## 4. Linux 常用基础命令（必会）
```bash
pwd
# 显示当前所在目录

ls
# 查看当前目录内容

cd 目录名
# 进入目录

cd ~
# 回到 home 目录

mkdir c_exam
# 创建文件夹
```
---
# 第二部分：nano 编辑器（考试兜底技能）
## 5. 为什么一定要会 nano？
- 考试机房常见
- 无需图形界面
- 简单可靠
- VS Code 不可用时的兜底方案
---
## 6. 使用 nano 编辑 C 程序
```bash
nano test.c
# 新建或编辑 test.c
```
### nano 常用快捷键（必须记）

|功能|快捷键|
|---|---|
|保存|Ctrl + O|
|确认|Enter|
|退出|Ctrl + X|
|剪切一行|Ctrl + K|
|粘贴|Ctrl + U|
|查找|Ctrl + W|

---
# 第三部分：gcc —— C 语言考试核心工具
## 7. 安装 C 语言工具（只需一次）
```bash
sudo apt install build-essential gdb
# 安装 gcc 与 gdb
```
---
## 8. C 语言考试标准编译流程
```bash
gcc test.c -o test
# 编译 C 程序

./test
# 运行程序
```
### 强烈推荐的防扣分写法
```bash
gcc test.c -std=c11 -Wall -Wextra -o test
# 使用 C11 标准并开启警告
```
---
# 第四部分：VS Code + WSL（效率篇）
## 9. 正确打开 VS Code（非常重要）
```bash
code .
# 从 WSL 打开 VS Code
```
左下角必须显示：
```
WSL: Ubuntu
```
---
## 10. Code Runner 插件（简化操作）
### 作用
- 一键完成：编译 + 运行
- 减少重复输入命令
### 使用方式
- 打开 `.c` 文件
- 点击 ▶ Run Code
- 或快捷键：`Ctrl + Alt + N`
⚠️ 本质仍然是：
```bash
gcc test.c -o test && ./test
```
👉 **考试时仍要会手动 gcc**

---
# 第五部分：gdb —— C 语言查错神器（重点）
## 11. gdb 是干什么的？
👉 **让程序“一步一步运行”，并查看变量值**
适合排查：
- 未初始化变量
- 循环错误
- 指针问题
- 数组越界
---
## 12. 使用 gdb 的基本流程
### 12.1 编译时必须加 -g
```bash
gcc test.c -g -Wall -o test
# -g：生成调试信息
```
---
### 12.2 启动 gdb
```bash
gdb ./test
# 进入 gdb 调试环境
```

---
### 12.3 gdb 必会命令
```text
b main     # 在 main 处打断点
r          # 运行程序
n          # 单步执行（不进函数）
s          # 单步进入函数
p 变量名   # 查看变量值
c          # 继续运行
q          # 退出 gdb
```
---
## 13. 真题演示：gdb 查错全过程
### 题目
> 输入 n，计算 1 + 2 + … + n
### 错误代码（高频）
```c
int sum;
for (int i = 1; i <= n; i++)
{
    sum += i;   // ❌ 未初始化
}
```
---
### 调试步骤
```bash
gcc sum.c -g -Wall -o sum
# 编译并生成警告
```

```bash
gdb ./sum
```

```text
b main
r
n
p sum
# 看到 sum 是随机值
```
👉 **定位错误：sum 未初始化**
修正：
```c
int sum = 0;
```
---
## 14. VS Code 中使用 gdb（图形化）
- 点击行号左侧打断点
- 按 F5 启动调试
- 悬停即可查看变量
👉 本质仍是 gdb，只是更直观
---
# 第六部分：CMake（作业管理工具）
## 15. CMake 在考试中的定位
- ❌ 考试一般不用
- ✅ 作业 / 多文件项目非常有用
---
## 16. 最简单的 CMake 模板（新手）
### 项目结构
```text
c_work/
├── CMakeLists.txt
├── main.c
├── add.c
├── add.h
```
---
### CMakeLists.txt
```cmake
cmake_minimum_required(VERSION 3.16)

project(c_work C)

set(CMAKE_C_STANDARD 11)
set(CMAKE_BUILD_TYPE Debug)

add_executable(c_work
    main.c
    add.c
)
```
---
### 使用流程
```bash
mkdir build
cd build
cmake ..
cmake --build .
```

```bash
./c_work
# 运行程序
```
---
# 第七部分：学习与考试建议（总结）
## 17. 学习优先级（非常重要）
```text
gcc + -Wall
→ gdb 查错
→ VS Code 提效
→ CMake 管理作业
```
---
## 18. 考试生存法则（记住）
1. 代码写在 `/home`
2. 用 gcc，不用 g++
3. 编译一定看警告
4. 平时用 gdb 练思路
5. 考试时“脑中模拟 gdb”
---
